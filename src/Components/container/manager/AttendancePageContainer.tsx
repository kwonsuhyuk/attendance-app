import React from "react";

const AttendancePageContainer = ({ children }: { children: React.ReactNode }) => {
  return <div className="w-full flex-1">{children}</div>;
};

export default AttendancePageContainer;
